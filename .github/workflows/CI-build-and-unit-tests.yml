name: CI-build

run-name: $CI build

on: push

env:
  RUST_BACKTRACE: 1

jobs:
  check-cargo-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cargo build --release --all-features